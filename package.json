{
  "name": "orchestrai",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter @orchestrai/web dev",
    "build:web": "pnpm --filter @orchestrai/web build"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
